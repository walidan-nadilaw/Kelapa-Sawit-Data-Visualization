line chart
    define plotly.d3.csv( dataset, function(err,rows))
    handle errors

    filter map with province
    set tahun as a list of tahun in every row
    set upah as list upah in every row, float parsed


    set data
        x = tahun
        y = upah
        tipe line = scatter
        mode line = lines+marker
        nama = untuk legend (jika)
        karakteristik line
            warna : blue
            tebal : 2

    layout dari grafik

        judul grafik 
        label di xaxix
        label di yaxix
        font label

    let config 
    run plotly 


    apa bedanya kalau responsive tru/false

bar chart
    Plotly.d3.csv(dataset, function(err,rows) {
        
        if (err){
            console.error(err);
            return;
        }

    
    }) 